<% content_for(:title, t('.title')) %>

<h2 class="text-xl font-bold mb-4">現在のみんなのひとこと（<%= t("time_slots.#{@slot}") %>）</h2>

<% if @posts.any? %>
  <ul>
    <% @posts.each do |post| %>
      <li class="block bg-white text-black-900 text-lg font-semibold px-6 py-3 mb-3 rounded-full shadow-md hover:shadow-lg transition">
        <p><%= post.user.name %>さんのひとこと</p>
        <p><%= post.tweet %></p>
        <small><%= post.created_at.strftime("%H:%M") %></small>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>ひとことはまだありません。</p>
<% end %>
<div class="sticky bottom-4 flex justify-center mt-8">
<%= link_to '今日のひとこと', posts_path, class: "bg-blue-500 hover:bg-blue-600 text-white py-2 px-4 rounded transition" %>
</div>